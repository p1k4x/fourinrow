# Backend base URL (no trailing slash). CORS already allows Vite on :5173 / :4173.
# Local default: http://localhost:5275
# Same-origin (Vite proxy or Docker nginx): leave empty — VITE_API_BASE_URL=
VITE_API_BASE_URL=http://localhost:5275
